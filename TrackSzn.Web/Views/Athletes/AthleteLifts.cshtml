@model TrackSzn.ViewModels.Athletes.LiftsViewModel

@{
    ViewBag.Title = "Athlete Lifts";
    ViewBag.Description = "Use this page to navigate to specific athlete lifting information.";
}

<div class="card-deck">
    @foreach (var graduationYearGroup in Model.AthletesByGraduationYear)
    {
        <div class="card">
            <div class="card-header">
                @graduationYearGroup.Key
            </div>
            <ul class="list-group list-group-flush">
                @foreach (var athlete in graduationYearGroup)
                {
                    <li class="list-group-item">
                        @Html.ActionLink(athlete.Name, "AthleteLiftList", "Athletes", new { id = athlete.Id }, new { })
                    </li>
                }
            </ul>
        </div>
    }
</div>
