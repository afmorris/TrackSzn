@model TrackSzn.ViewModels.Meets.CreateViewModel

@{
    ViewBag.Title = "Create a Meet";
    ViewBag.Description = "Use this page to create a new meet.";
}

@using (Html.BeginForm("Create", "Meets", FormMethod.Post, new { }))
{
    @Html.AntiForgeryToken()
    if (ViewData.ModelState[""] != null && ViewData.ModelState[""].Errors.Any())
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <h3 class="alert-heading">Whoops.</h3>
            @Html.ValidationSummary(false, string.Empty, new { @class = "m-0" })
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }

    <div class="form-group">
        @Html.LabelFor(x => x.Name)
        @Html.TextBoxFor(x => x.Name, new { @class = "form-control", autofocus = "autofocus", tabindex = 1 })
    </div>

    <div class="form-group">
        @Html.LabelFor(x => x.Date)
        @Html.TextBoxFor(x => x.Date, new { @class = "form-control", autofocus = "autofocus", tabindex = 2 })
    </div>

    <button type="submit" class="btn btn-outline-info" tabindex="3">Create Meet</button>
}

<hr />
@Html.ActionLink("Back to Meets", "Index", "Meets", null, new { @class = "btn btn-outline-primary" })
